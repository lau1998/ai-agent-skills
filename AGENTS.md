# 项目协作规则

## Skill 管理

- 新增、创建、分类、登记或更新 Skill 时，必须先读取并遵循 `skills/development/create-skill/SKILL.md`。
- 项目只使用 `development` 和 `other` 两个分类目录。
- 分类以主要交付物为准：主要产出为代码或工程配置时归入 `development`，其余归入 `other`。
- 新 Skill 通过校验后，必须登记到根目录 `catalog.yaml`。

## 代码注释

- 修改代码时，关键和重要逻辑必须添加简短注释。
- 函数和方法必须使用文档注释说明职责、参数、返回值或异常。

